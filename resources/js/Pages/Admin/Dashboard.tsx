import Layout from "@/Layouts/Admin/AdminLayout";

const AdminDashboard = () => {
    return (
        <Layout header={{ Parent:'Dashboard' }}>
            <h1>Admin Dashboard</h1>
        </Layout>
    );
};

export default AdminDashboard;
